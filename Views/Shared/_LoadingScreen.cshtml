<div id="loading-screen" class="fixed inset-0 bg-gray-800 bg-opacity-75 flex justify-center items-center z-50">
    <div class="text-center text-white p-6 bg-gray-900 rounded-lg shadow-lg">
        <!-- Loading Text -->
        <h2 class="text-2xl font-semibold mb-4 animate-pulse">Loading, please wait...</h2>
        
        <!-- Spinner Centered -->
        <div class="flex justify-center items-center">
            <div class="animate-spin rounded-full h-16 w-16 border-t-4 border-white border-solid"></div>
        </div>

        <!-- Optional: Add additional text or animation -->
        <p class="mt-4 text-lg">Just a moment, we're almost there!</p>
    </div>
</div>
